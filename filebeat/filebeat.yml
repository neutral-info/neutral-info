filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/filebeat/ptt/*.log
output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  username: "elastic"
  password: "changeme"
setup.kibana:
  host: "kibana:5601"
  username: "elastic"
  password: "changeme"
